RewriteEngine Off

# video-log.txt morava aqui e tinha bloqueio de URL direta (<Files>) —
# mesmo padrão de hotmart/.htaccess (config.php / _conexao.php). Removido
# porque o arquivo agora vive fora de public_html (ver comentário em
# video-log.php), então já não é alcançável por URL nenhuma; nada a
# bloquear neste diretório.
